extend layout

block content
    h1= article.title
    h5 Written bu #{article.author}
    p= article.body
    hr
    a.btn.btn-default(href='/articles/edit/'+article._id) Edit
    a.btn.btn-danger#delete-article(href='#', dataid=article._id) Delete